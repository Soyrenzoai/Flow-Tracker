// Configuración de conexión al servidor
export const SERVER_CONFIG = {
  // Cambia esto a true cuando tengas tu servidor funcionando
  USE_SERVER: false, 
  
  // La dirección IP o URL de tu servidor (Ej: 'http://192.168.1.50:3000' o 'https://mi-api.com')
  API_URL: 'http://localhost:3000/api',
  
  // Clave de seguridad simple (opcional, tu servidor debería verificarla)
  API_KEY: 'mi-clave-secreta' 
};